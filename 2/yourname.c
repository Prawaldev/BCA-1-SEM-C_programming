#include<stdio.h>
int main() {
    int n,a,add;
    printf("Enter your name, age, address: ");
    scanf("%d%d%d",&n,&a,&add);
    printf("name: %d\n age: %d\n address: %d\n",n,a,add);
    return 0;
}